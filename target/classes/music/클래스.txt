<-- DB와의 상호작용을 위한 로직표현 호출 -->
1. Mapper 인터페이스 작성
    (/mapper/mypageDao.java)

2. Mapper XML  파일 작성
    (SQL문 작성)
    <select...> </select>


<-- 비즈니스 로직 처리 -->
3. Service 인터페이스 작성
    Service 인터페이스에는 메소드 선언
    List<User> getUserById(Integer id);

4. Service 구현 클래스 작성
    private final UserMapper dao;

    @Override
    public User getUserById(Integer id) {
        retrun dao.findById(id);
    }


5. Controller클래스 작성
    Service를 주입받아 비즈니스 로직을 호출해, 클라이언트의 요청에 응답

